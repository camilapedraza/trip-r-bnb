
<div class="container">
  <ul>
    <% @cars.each do |car| %>
      <li>
        <%= link_to car.name, car_path(car) %>
      </li>
    <% end %>
  </ul>
  <a class="btn btn-primary mt-3" href="<%= new_car_path %>">Create a new car</a>
</div>

<h1>Find the perfect 4L for you</h1>
<div class="container">
  <div style="width: 100%; height: 600px;"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
  <ul>
  <div class="cards">
    <% @cars.each do |car| %>
      <div class="card-trip">
      <%= car.photos.attached? ? cl_image_tag(car.photos.first.key) : image_tag('4L.jpg') %>
      <div class="card-trip-infos">
        <%= link_to car_path(car), :action => 'go' do %>
        <div>
          <h2><%= car.name %></h2>
          <p><%= car.description %></p>
        </div>
        <% end %>
        <h2 class="card-trip-pricing"><%= car.price %>€/jour</h2>
      <img src="<%= cl_image_path car.user.photo.key %>" class="card-trip-user avatar-bordered" />

      </div>
      </div>
    <% end %>
  </div>
  </ul>

  <a><%= link_to 'Rent your 4L', new_car_path, class: "btn btn-info" %></a>
</div>

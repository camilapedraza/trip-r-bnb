<h1>Check out our 4L Car Rental !</h1>


<ul>
  <% @cars.each do |car| %>
    <li><%= link_to car.name, car_path(car) %></li>
  <% end %>
</ul>

<a><%= link_to 'Create a new car', new_car_path if policy(Car).new?%></a>

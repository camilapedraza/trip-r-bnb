<%= simple_form_for [@car, @booking],
  data: {
    insert_total_price_target: "form",
    action: "change->insert-total-price#price"} do |f|%>
  <%= f.input :starting_date, start_year: Date.today.year, end_year: Date.today.year + 5, input_html: { id: "range_start" } %>
  <%= f.input :ending_date, start_year: Date.today.year, end_year: Date.today.year + 5, input_html: { id: "range_start" } %>
  <%= f.button :submit, class: "btn btn-info rounded-4", value: "Book car"%>
<% end %>
